name: first git 
on: 
  push: 
    branches: ['main']
jobs:
  run-app: 
    runs-on: self-hosted
    steps: 
      - uses: actions/checkout@v4
      - run: npm install express mongoose cors dotenv
      - run: npm install pm2 -g 
      - run: pm2 start server.js --name kunj  
      
